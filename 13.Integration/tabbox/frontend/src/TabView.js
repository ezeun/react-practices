import React from 'react';

function TabView(props) {
    return (
        <div>
            <span>hello</span>
            <span>{props.contents}</span>
        </div>
    );
}

export default TabView;